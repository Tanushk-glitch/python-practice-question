#Count Vowels Using Loop & Condition

text = "python"
vowels = "aeiou"
count = 0

for ch in text:
    if ch in vowels:
        count += 1

print(count)
# The code counts the number of vowels in the string "python" using a loop and condition.